source 'https://rubygems.org'

ruby ">= 3.1"  # Jekyll 4.4+ requires Ruby 3.1 or newer (e.g. `brew install ruby@3.2`)

group :jekyll_plugins do
    gem 'jekyll'
    gem 'jekyll-email-protect'
    gem 'jekyll-scholar'
    gem 'jekyll-sitemap'
    gem 'jemoji'
    gem 'unicode_utils'
    gem 'webrick'
	gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]
end
